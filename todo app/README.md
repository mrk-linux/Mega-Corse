to-do app

we will build in 3 ways to-do app

command, app,web app

to-do.py fainal app

P-TO-DO.ipynb steps to build and progress

For the web version to work, you first need to run the following command:

```bash
streamlit run "program_name"
```

If you get an error, navigate to the program's folder first, then run it.

---
